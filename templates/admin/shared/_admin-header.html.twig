<div class="d-flex justify-content-between my-4">
    <h1>Tableau de bord admin</h1>

    <div>

        {% set pillStats = {
            'utilisateurs inscrits': users,
            'evenements': events,
        } %}

        {% for name, pillStat in pillStats %}

            {{ include('admin/shared/_pill.html.twig', {
                content: pillStat|length ~ ' ' ~ name
            }) }}

        {% endfor %}
    </div>
</div>

<nav class="navbar navbar-light bg-light navbar-expand-lg bg-body-tertiary rounded-pill my-5 px-5">
    <div class="container-fluid">
        <a class="navbar-brand fw-bolder" href="#">ADMIN</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNavDropdown"
                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">

                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/admin/dashboard">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/admin/event/index">Evenements</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/admin/user/index">Utilisateurs</a>
                </li>
            </ul>
        </div>
    </div>
</nav>